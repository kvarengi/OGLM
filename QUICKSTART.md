# OGLM Azimuth Terminal - Quick Start
## Быстрый старт для @fractal_whale

### 🚀 Запуск (выбери свою платформу)

#### Mac/Linux
```bash
./run_terminal.sh
# или
python3 azimuth_terminal.py
```

#### Windows
```powershell
python azimuth_terminal.py
```

#### Termux (Android)
```bash
python azimuth_terminal.py
```

---

### 📱 Установка на Termux (шаг за шагом)

```bash
# 1. Открыть Termux
# 2. Скопировать и вставить по очереди:

pkg update
pkg install python git
git clone https://github.com/kvarengi/OGLM.git
cd OGLM
python azimuth_terminal.py
```

**Готово!** Терминал запущен.

---

### 🎯 Как использовать

```
1. Введи азимут (число):
   -99   = падение на 99%
   +50   = рост на 50%
   +1    = рост на 1%
   
2. Жми Enter (или добавь reasoning)

3. Укажи горизонт в днях (или Enter для 7 дней)

4. Получи результат!
```

---

### 💡 Команды

В поле "Азимут:" вместо числа можешь ввести:

```
stats    → статистика
chart    → график
history  → вся история
clear    → очистить экран
exit     → выход
```

---

### 📂 Где данные?

```
~/.oglm/azimuth_predictions.json
~/.oglm/config.json
```

Можешь бэкапить, синхронизировать, редактировать.

---

### 🔥 Pro tips

**Alias для быстрого запуска:**
```bash
echo "alias whale='python3 ~/OGLM/azimuth_terminal.py'" >> ~/.bashrc
source ~/.bashrc
# Теперь просто:
whale
```

**Работа с нескольких устройств:**
```bash
# Синхронизация через git
cd ~/.oglm
git init
git add .
git commit -m "my predictions"
git remote add origin YOUR_PRIVATE_REPO
git push

# На другом устройстве:
cd ~/.oglm
git pull
```

**Backup:**
```bash
cp -r ~/.oglm ~/.oglm.backup
```

---

### ❓ FAQ

**Q: Терминал закрылся, данные потерялись?**  
A: Нет! Все сохраняется в `~/.oglm/`. Просто запусти снова.

**Q: Хочу начать сначала**  
A: `rm -rf ~/.oglm/` и запусти терминал снова

**Q: Можно ли торговать реально?**  
A: Пока это симуляция. Реальная торговля в v2.0

**Q: Работает офлайн?**  
A: Да! Никакого интернета не нужно.

---

### 🎮 Примеры сессий

#### Сессия 1: Первый прогноз
```
Азимут: -99
Reasoning: Зловещая долина после запуска
Горизонт: 7

→ Результат через 7 дней
→ Данные сохранены
```

#### Сессия 2: Проверка статистики
```
Азимут: stats

→ Точность: 100%
→ Total P&L: -99%
```

#### Сессия 3: Второй прогноз
```
Азимут: +1
Reasoning: Медленное восстановление после дна
Горизонт: 7

→ Результат
→ Обновленная статистика
```

---

**Вот и все! Теперь ты @fractal_whale с собственным торговым терминалом** 🐋

*Запускай и делай прогнозы!*

